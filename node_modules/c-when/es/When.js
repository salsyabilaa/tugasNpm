var isFunction = function isFunction(obj) {
  return typeof obj === 'function';
};

var invokeOrReturn = function invokeOrReturn(obj) {
  return isFunction(obj) ? obj() : obj;
};

var When = function When(_ref) {
  var predicate = _ref.predicate,
      children = _ref.children,
      render = _ref.render;
  return predicate ? invokeOrReturn(render || children) : null;
};

export default When;